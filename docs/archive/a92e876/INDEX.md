# Pre-0.6.0 documentation set

Archived at commit `a92e876`. Nothing here is operational.

Superseded by the root `README.md`, `ARCHITECTURE.md`, and `AGENT.md`.

Several files in this set were found to disagree with the code, which is why
they were replaced wholesale rather than edited — a corrected copy would still
have carried the structure that let the drift happen.

Cross-references inside these files are left as they were written, and point at
the pre-archive layout. They are a record, not a map.
